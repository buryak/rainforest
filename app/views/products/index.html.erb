<h1>Rainforest</h1>

<p><%= link_to "Add New Product", new_product_path %></p>
<%  @products.each do |product| %>
	<p><%= link_to product.name, product %><br>
	<%= product.description %><br>
	<%= product.price_in_cents %></p>
	<% end %>

